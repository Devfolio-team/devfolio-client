import { create } from '@storybook/theming';

export default create({
  base: 'light',
  brandTitle: 'Devfolio',

  // UI
  appBg: '#fff',
  appContentBg: '#fefefe',
  appBorderColor: '#e4e4e4',
  appBorderRadius: 4,
});
